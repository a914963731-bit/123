{
  "name": "ai-tool",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo done",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
